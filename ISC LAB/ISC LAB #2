1. Sort the file RED and Display it on the screen
  sort RED.txt
2. Sort the file RED and store it in another file 
  sort RED>txt > NEWRED.txt
3. Sort the file RED in reverse order and store it in reversed order
  sort -r RED.txt > REVERSEDRED.txt
4. Remove Consecutive Dupilcate in Given file 
  uniq RED.txt
5. Display the first 10 lines lf a File
  Head RED.txt
6. Display the last 10 lines in File
  tail RED.txt
7. Display the first 3 lines in a file
  head -3 RED.txt
8. Display the last 3 Lines in a file
  tail -3 RED.txt
9. Create a file student with the Fields "Name, Class" and another file called studentMarks with Fields "Roll Number, Mark1, Mark2"
 see @aditya
9a.Display onelu Roll Nukmber and Mark1
  awk {print $1, $2} studentMarks.txt
9b. Combine these two Files
  paste Student.txt studentMarks.txt
9c. To edit Folder use 
  nano "fileName"
10. Create a file Fruitfile.txt and put 20 redundant entries including the word apple
        a. Search word appple in Fruitfile.txt
            grep "apple" Fruitfile.txt
        b. Count the number of times apple is found in the file
            grep "apple" fruitfile.txt
        c. Display the lines which dont contain the word apple 
            grep -v "apple" fruitfile.txt
